<div id="erreurmaili"></div>
<div id="erreurpasswordi"></div>
<div id="erreurnomi"></div>
<div id="erreurville"></div>
<div id="erreurtel"></div>
	<form action="" method="post" name="form1" id="form1" class="form1">
		<label for="mail_inscript">Votre adresse mail</label>
		<input type="text" id="mail_inscript" name="mail_inscript" placeholder="Ex : jean.dupont@achetervehicule.com"/>
		<label for="password_inscript">Mot de passe</label>
		<input type="password" name="password_inscript" id="password_inscript"/>
	<label for="nom_inscript">Votre nom et prénom</label>
	<input type="text" name="nom" id="nom_inscript" placeholder="Ex : Dupont"/>
	<label for="ville">Votre ville</label>
	<input type="text" name="ville" id="ville" placeholder="Ex : Paris"/>
	<input type="hidden" name="cp" id="cp"/>
	<label for="tel">Votre téléphone</label>
	<input type="text" name="tel" id="tel" placeholder="Ex : 061122334455"/>
	<input id="inscription" type="submit" value="S'inscrire">
	</form>
<script type="text/javascript">
function surligne(champ, erreur)
{
   if(erreur)
      champ.style.borderColor = "#df8f90";
   else
      champ.style.borderColor = "";
}
$(function(){ 
   $('#form1')
      .bind('submit',function(event) {
      return verifSubmit(form1);
   });
	$('#mail_inscript')
      .bind('blur',function(event) {
      verifMailI();
   });
   $('#password_inscript')
      .bind('blur',function(event) {
      verifPasswordEmptyi();
   });
   $('#nom')
      .bind('blur',function(event) {
      verifNomEmpty();
   });
   $('#ville')
      .bind('blur',function(event) {
      verifVilleEmpty();
   });
   $('#tel')
      .bind('blur',function(event) {
      verifTelephoneEmpty();
   });
});

function verifMailI()
{
   var champ = document.getElementById("mail_inscript");
   var regMailExpression = /^[a-zA-Z0-9._-]+@[a-z0-9._-]{2,}\.[a-z]{2,4}$/;

   if(champ.value.length < 1)
   {
    surligne(champ, true);
	  document.getElementById("erreurmaili").innerHTML="<div class='notif error'>Le mail est obligatoire</div>";
	  return false;
   }
   else if(!regMailExpression.test(champ.value)) {
   	  surligne(champ,true);
   	  document.getElementById("erreurmaili").innerHTML="<div class='notif error'>L'adresse mail est invalide</div>";
   	  return false;
   }
   else
   {
      surligne(champ, false);
      document.getElementById("erreurmaili").innerHTML="";
      return true;
   }
}

function verifPasswordEmptyi()
{
   var password = document.getElementById("password_inscript");

   if(password.value.length < 1)
   {
     surligne(password, true);
     document.getElementById("erreurpasswordi").innerHTML="<div class='notif error'>Le mot de passe est obligatoire</div>";
     return false;
   }
   else
   {
      surligne(password, false);
      document.getElementById("erreurpasswordi").innerHTML="";
      return true;
   }
}

function verifNomEmpty()
{
   var nom = document.getElementById("nom_inscript");

   if(nom.value.length < 1)
   {
     surligne(nom, true);
     document.getElementById("erreurnomc").innerHTML="<div class='notif error'>Le nom est obligatoire</div>";
     return false;
   }
   else
   {
      surligne(nom, false);
      document.getElementById("erreurnomc").innerHTML="";
      return true;
   }
}

function verifVilleEmpty()
{
   var ville = document.getElementById("ville");

   if(ville.value.length < 1)
   {
     surligne(ville, true);
     document.getElementById("erreurville").innerHTML="<div class='notif error'>La ville est obligatoire</div>";
     return false;
   }
   else
   {
      surligne(ville, false);
      document.getElementById("erreurville").innerHTML="";
      return true;
   }
}

function verifTelephoneEmpty()
{
   var tel = document.getElementById("tel");
   var regMailExpression = /^[0-9]+$/;

   if(tel.value.length < 1)
   {
     surligne(tel, true);
     document.getElementById("erreurtel").innerHTML="<div class='notif error'>Le téléphone est obligatoire</div>";
     return false;
   }
   else if(!regMailExpression.test(tel.value)) {
      surligne(tel,true);
      document.getElementById("erreurtel").innerHTML="<div class='notif error'>Le téléphone est invalide</div>";
      return false;
   }
   else
   {
      surligne(tel, false);
      document.getElementById("erreurtel").innerHTML="";
      return true;
   }
}

function verifSubmit(f) 
{
   var mailOk = verifMailI();
   var mdpOk = verifPasswordEmptyi();
   var nomOk = verifNomEmpty();
   var villeOk = verifVilleEmpty();
   var telOk = verifTelephoneEmpty();
   
   if(mailOk && mdpOk && nomOk && villeOk && telOk)
      return true;
   else
   {
      alert("Veuillez remplir correctement tous les champs");
      return false;
   }
}
</script>